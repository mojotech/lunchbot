<section id="torch-toolbar">
  <div class="torch-container">
    <%= link "New Extra", to: Routes.admin_extra_path(@conn, :new) %>
  </div>
</section>
<section id="torch-index-content">
  <div class="torch-container">
    <section id="torch-filters">
      <h3>Find Extras</h3>
      <%= form_tag @conn.request_path, method: :get, id: "torch-filters-form" do %>
        
          <div class="field">
            <label>Name</label>
            <%= filter_select(:extra, :name, @conn.params) %>
            <%= filter_string_input(:extra, :name, @conn.params) %>
          </div>
        
        
        
        
          <div class="field">
            <label>Item</label>
            <%= number_filter_select(:extra, :item_id, @conn.params) %>
            <%= filter_number_input(:extra, :item_id, @conn.params) %>
          </div>
        
        <button type="submit" class="torch-button">Search</button>
        <%= link "Clear Filters", to: Routes.admin_extra_path(@conn, :index) %>
      <% end %>

    </section>

    <section id="torch-table">
    <%= if length(@extras) > 0 do %>
      <table>
        <thead>
          <tr>
            
              <th><%= table_link(@conn, "Name", :name) %></th>
            
              <th><%= table_link(@conn, "Item", :item_id) %></th>
            
            <th><span>Actions</span></th>
          </tr>
        </thead>
        <tbody>
          <%= for extra <- @extras do %>
            <tr>
              
                <td><%= extra.name %></td>
              
                <td><%= extra.item_id %></td>
              
              <td class="torch-actions">
                <span><%= link "Show", to: Routes.admin_extra_path(@conn, :show, extra) %></span>
                <span><%= link "Edit", to: Routes.admin_extra_path(@conn, :edit, extra) %></span>
                <span><%= link "Delete", to: Routes.admin_extra_path(@conn, :delete, extra), method: :delete, data: [confirm: "Are you sure?"] %></span>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= render Torch.PaginationView, "_pagination.html", assigns %>
    <% else %>
      <p class="torch-no-data">No Extras match your search.</p>
    <% end %>
    </section>
  </div>
</section>